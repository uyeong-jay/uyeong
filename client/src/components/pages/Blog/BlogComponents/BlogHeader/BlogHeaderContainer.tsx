import React from 'react';
import BlogHeaderPresenter from './BlogHeaderPresenter';

const BlogHeaderContainer = () => {
  return <BlogHeaderPresenter />;
};

export default React.memo(BlogHeaderContainer);
