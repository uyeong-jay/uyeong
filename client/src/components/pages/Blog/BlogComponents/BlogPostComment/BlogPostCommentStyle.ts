import styled from '@_settings/styled';

const Layout = styled.section`
  border: 1px solid green;
`;

const ReplyGourp = styled.div`
  border: 1px solid red;
  width: 100%;
  padding-left: 50px;
`;

export const SECTION = {
  Layout,
};

export const DIV = {
  ReplyGourp,
};
