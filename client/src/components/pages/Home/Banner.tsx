import React from 'react';
import styled from '@_settings/styled';

const StyledSection = styled.section`
  border: 1px solid black;
  display: flex;
  justify-content: center;
  align-items: center;
  width: 750px;
  height: 200px;
`;

const Banner = () => {
  return <StyledSection>Banner</StyledSection>;
};

export default Banner;
